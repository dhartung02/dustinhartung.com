# Routing Overrides

No overrides yet. See ../portfolio/agents/shared/workflow-routing.md for the shared default
matrix. Add project-specific narrowing here (e.g. this project always requires Architect for
small bug fixes) — overrides should narrow or adjust conditions, not drop QA or skip
human-gated actions.
